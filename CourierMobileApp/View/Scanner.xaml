<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:zxing="clr-namespace:ZXing.Net.Maui.Controls;assembly=ZXing.Net.MAUI.Controls"
             xmlns:viewmodel="clr-namespace:CourierMobileApp.ViewModels"
             x:DataType="viewmodel:ScannerViewModel"
             x:Class="CourierMobileApp.View.Scanner"
             Shell.NavBarIsVisible="False"
             Title="Scanner">
    <ScrollView Background="{StaticResource LoggedInBgDark}">
        <Grid RowDefinitions="*, 500, *" Padding="0,0" VerticalOptions="Center">
            <zxing:CameraBarcodeReaderView
                Grid.Row="1"
                x:Name="cameraBarcodeReaderView"
                BarcodesDetected="BarcodesDetected" HeightRequest="500" />
            <Label TextColor="White" Grid.Row="2" Text="{Binding Label}" />
            <Frame HeightRequest="200" BackgroundColor="#4dffffff" 
                   BorderColor="Black" Grid.Row="1" x:Name="scanFrame" Padding="10, 2">
                <BoxView HeightRequest="2" x:Name="scanLine" VerticalOptions="Start"
                         HorizontalOptions="Center" BackgroundColor="Black" />
            </Frame>
        </Grid>
    </ScrollView>
    
</ContentPage>